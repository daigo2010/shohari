<div class="well">
<p id="descriptionArea"><% if @exist %>これがあなたの結果です！ <br /><br />それぞれの窓にマウスポインタを合わせると、詳細な説明が表示されます。<% else %>他の人からの評価がないため、結果を表示できません。友達を誘ってみましょう！
<% end %></p>
</div>

<% if @exist %>
<table align="center">
<tr>
<td>
<table class="table table-bordered window" id="openSelf">
    <tr><td style="text-align : center;" >
        <dl>
        <% @open_self.each_pair do |key, value| %>
            <li><%= value %></li>
        <% end %>
        </dl>
    </td></tr>
</table>
</td>
<td>
<table class="table table-bordered window" id="bindSelf">
    <tr><td style="text-align : center;">
        <dl>
        <% @bind_self.each_pair do |key, value| %>
            <li><%= value %></li>
        <% end %>
        </dl>
    </td></tr>
</table>
</td>
</tr>
<tr>
<td>
<table class="table table-bordered window" id="hiddenSelf">
    <tr><td style="text-align : center;">
        <dl>
        <% @hidden_self.each_pair do |key, value| %>
            <li><%= value %></li>
        <% end %>
        </dl>
    </td></tr>
</table>
</td>
<td>
<table class="table table-bordered window" id="unknownSelf">
    <tr><td style="text-align : center;">
        <dl>
        <% @unknown_self.each_pair do |key, value| %>
            <li><%= value %></li>
        <% end %>
        </dl>
    </td></tr>
</table>
</td>
</tr>
</table>
<% end %>
<%= link_to 'トップに戻る', '/', :class => 'btn' %>  
<script type="text/javascript">
(function() {
    var defaultDescription = "これがあなたの結果です！ <br /><br />それぞれの窓にマウスポインタを合わせると、詳細な説明が表示されます。";
    var openSelfDescription = "開放の窓です。これは、自分も友達も知っているあなたの性格です。<br /><br />この窓が大きいほど、自己開示が進んでいて、ありのままの自分を出せていることになります。";
    var bindSelfDescription = "盲点の窓です。これは、友達が思っているあなたの性格です。<br /><br />あなたは気づいていないかもしれませんが、外から観るとあなたはこのような性格をしているということになります。";
    var hiddenSelfDescription = "秘密の窓です。友達は知らないけれどもあなた自身は自分をこういう性格だと思っています。<br /><br />自覚のあるなしを問わず、結果的にああなたはこの性格を他の人から隠していることになります。";
    var unknownSelfDescription = "未知の窓です。あなたも知らないし、友達も知らないあなたの性格です。<br /><br />この性格はあなたの中にあるかもしれないし、無いかもしれません。";
    $("#openSelf").mouseover(function() {
        $("#descriptionArea").html(openSelfDescription);
    });
    $("#openSelf").mouseout(function() {
        $("#descriptionArea").html(defaultDescription);
    });
    $("#bindSelf").mouseover(function() {
        $("#descriptionArea").html(bindSelfDescription);
    });
    $("#bindSelf").mouseout(function() {
        $("#descriptionArea").html(defaultDescription);
    });
    $("#hiddenSelf").mouseover(function() {
        $("#descriptionArea").html(hiddenSelfDescription);
    });
    $("#hiddenSelf").mouseout(function() {
        $("#descriptionArea").html(defaultDescription);
    });
    $("#unknownSelf").mouseover(function() {
        $("#descriptionArea").html(unknownSelfDescription);
    });
    $("#unknownSelf").mouseout(function() {
        $("#descriptionArea").html(defaultDescription);
    });

})();
</script>
